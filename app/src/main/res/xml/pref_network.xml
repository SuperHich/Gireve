<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
    <com.dotit.gireve.ihm.views.InfoPreference
        android:key="@string/pref_key_ip_address"
        android:title="@string/pref_title_ip_address"
        android:defaultValue="@string/pref_default_ip_address"
        android:negativeButtonText="@null"
        android:positiveButtonText="@android:string/ok"/>

    <com.dotit.gireve.ihm.views.InfoPreference
        android:key="@string/pref_key_fault_response"
        android:title="@string/pref_title_fault_response"
        android:defaultValue="@string/pref_default_fault_response"
        android:negativeButtonText="@null"
        android:positiveButtonText="@android:string/ok"/>

</PreferenceScreen>
